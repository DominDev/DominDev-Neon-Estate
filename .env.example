# ===========================================
# NEON ESTATE - Backend Environment Variables
# ===========================================
#
# INSTRUKCJA KONFIGURACJI:
# 1. Skopiuj ten plik jako `.env` w głównym katalogu projektu
# 2. Wypełnij wartości swoimi danymi (nie commituj pliku .env do git!)
# 3. Uruchom `npm install` aby zainstalować zależności
# 4. Uruchom `npm run dev` dla developmentu lub `npm start` dla produkcji
#
# ===========================================

# --- GEMINI API CONFIGURATION ---
# Uzyskaj klucz API z: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Wersja modelu Gemini (domyślnie: gemini-2.5-flash-preview-09-2025)
GEMINI_MODEL=gemini-2.5-flash-preview-09-2025

# --- SERVER CONFIGURATION ---
# Port serwera (domyślnie: 3000)
PORT=3000

# Environment: development | production
NODE_ENV=development

# --- CORS CONFIGURATION ---
# Dozwolone originy (oddziel przecinkami dla wielu)
# Development: http://localhost:5500,http://127.0.0.1:5500
# Production: https://neon-estate.com
ALLOWED_ORIGINS=http://localhost:5500,http://127.0.0.1:5500

# --- RATE LIMITING ---
# Maksymalna liczba requestów na IP w ciągu windowMs
RATE_LIMIT_MAX_REQUESTS=10

# Okno czasowe w milisekundach (domyślnie: 15 minut = 900000ms)
RATE_LIMIT_WINDOW_MS=900000

# --- API TIMEOUTS ---
# Timeout dla requestów do Gemini API (milisekundy)
GEMINI_TIMEOUT_MS=30000

# --- SECURITY ---
# Secret dla dodatkowych funkcji (opcjonalnie)
API_SECRET=your_random_secret_here
